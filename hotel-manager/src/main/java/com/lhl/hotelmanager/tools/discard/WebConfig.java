package com.lhl.hotelmanager.tools.discard;

/**
 * @program: hotel-manager
 * @Date: 2018/11/6 0006 下午 7:56
 * @Author: <.*)#)))<
 * @Description: JackJson 配置文件（目前仅处理日期）
 */
//@Configuration
public class WebConfig {
/*
    @Autowired
    private 2ObjectMapperBuilder 2ObjectMapperBuilder;

    @Bean
    public Mapping2HttpMessageConverter MappingJsonpHttpMessageConverter() {

        ObjectMapper mapper = 2ObjectMapperBuilder.build();

        // ObjectMapper为了保障线程安全性，里面的配置类都是一个不可变的对象
        // 所以这里的setDateFormat的内部原理其实是创建了一个新的配置类
        DateFormat dateFormat = mapper.getDateFormat();
        mapper.setDateFormat(new MyDateFormat(dateFormat));

        Mapping2HttpMessageConverter mappingJsonpHttpMessageConverter = new Mapping2HttpMessageConverter(
                mapper);
        return mappingJsonpHttpMessageConverter;
    }*/

}
